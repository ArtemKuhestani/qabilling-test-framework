# 📊 Презентация проекта: QA Billing Test Framework

## 🎯 Обзор проекта

**QA Billing Test Framework** — это автоматизированный тестовый фреймворк для тестирования REST API биллинговой системы мобильного оператора.

### Основная информация

- **Язык**: Java 21
- **Фреймворк тестирования**: TestNG 7.10.2
- **API клиент**: REST Assured 5.5.0
- **Система сборки**: Maven
- **Тип тестирования**: API Testing / Integration Testing

---

## 🏗️ Архитектура фреймворка

Фреймворк построен по паттерну **Page Object Model (POM)**, адаптированному для API тестирования:

```
qabilling-test-framework/
├── src/test/java/qa/
│   ├── api/              # API клиенты (аналог Page Objects)
│   │   ├── BalanceApi.java
│   │   ├── CounterApi.java
│   │   └── ProfileApi.java
│   ├── config/           # Конфигурация и константы
│   │   └── Config.java
│   ├── dto/              # Data Transfer Objects
│   │   ├── request/      # Модели запросов
│   │   └── response/     # Модели ответов
│   ├── helpers/          # Вспомогательные классы
│   │   └── AuthHelper.java
│   ├── tests/            # Тестовые классы
│   │   ├── BaseTest.java
│   │   ├── auth/         # Тесты авторизации
│   │   ├── balance/      # Тесты балансов
│   │   ├── counter/      # Тесты счетчиков
│   │   └── profile/      # Тесты профилей
│   └── utils/            # Утилиты
│       └── MsisdnGenerator.java
└── src/test/resources/
    └── testng.xml        # Конфигурация TestNG
```

---

## 🔑 Ключевые компоненты

### 1. API Клиенты

API клиенты инкапсулируют логику взаимодействия с конкретными эндпоинтами:

#### **ProfileApi** — управление профилями абонентов
- Создание профиля (POST)
- Получение всех профилей (GET)
- Получение по ID/MSISDN (GET)
- Обновление профиля (PUT)
- Удаление профиля (DELETE)

#### **BalanceApi** — управление балансами
- Получение всех балансов (GET)
- Получение баланса по ID профиля (GET)
- Обновление суммы на балансе (PUT)

#### **CounterApi** — управление счетчиками ресурсов
- Получение всех счетчиков (GET)
- Получение активных счетчиков (GET)
- Получение счетчика по ID профиля (GET)

### 2. BaseTest — базовый класс для всех тестов

Предоставляет:
- ✅ Автоматическую авторизацию с JWT токеном администратора
- ✅ Готовые экземпляры API клиентов
- ✅ Вспомогательные методы для проверки HTTP статусов
- ✅ Утилиты для извлечения данных из JSON ответов

### 3. Config — централизованная конфигурация

Хранит:
- 🌐 URL тестируемого API: `http://195.38.164.168:7173`
- 🔐 Учетные данные администратора
- 📍 Пути всех эндпоинтов
- 🔢 Значения по умолчанию (price plan ID, user ID)

### 4. DTO (Data Transfer Objects)

**Request DTO:**
- `LoginRequestDto` — данные для входа
- `SignUpRequestDto` — данные для регистрации
- `CreateProfileRequest` — параметры создания профиля
- `UpdateBalanceRequest` — обновление баланса

**Response DTO:**
- `ProfileResponse` — профиль абонента
- `BalanceResponse` — баланс абонента
- `CounterResponse` — счетчик ресурсов
- `ApiResponseDto` — стандартный ответ API

### 5. Утилиты

**MsisdnGenerator** — генерация уникальных номеров телефонов
- Формат: `996801234567` (код Кыргызстана + 7 случайных цифр)
- Гарантирует уникальность для каждого теста

**AuthHelper** — помощник авторизации
- Получение JWT токена администратора
- Автоматический логин при старте тестов

---

## 📊 Покрытие тестами

### Статистика

| Модуль | Позитивные тесты | Негативные тесты | Итого |
|--------|-----------------|------------------|-------|
| **Authentication** | 2 | 2 | **4** |
| **Profile** | 8 | 14 | **22** |
| **Balance** | 5 | 5 | **10** |
| **Counter** | 5 | 3 | **8** |
| **ВСЕГО** | **20** | **24** | **44** |

### Типы тестов

#### ✅ Позитивные тесты
- Успешное создание, чтение, обновление, удаление (CRUD)
- Получение списков и фильтрация
- Корректная обработка валидных данных

#### ❌ Негативные тесты
- Обработка несуществующих ID
- Валидация обязательных полей
- Проверка дубликатов (например, MSISDN)
- Проверка граничных значений
- Обработка некорректных форматов данных

---

## 🧪 Примеры тестов

### Пример 1: Создание профиля (позитивный тест)

```java
@Test
public void shouldCreateProfileWithValidData() {
    String msisdn = MsisdnGenerator.generateUniqueMsisdn();
    
    Response response = profileApi.createProfile(msisdn, 
            Config.DEFAULT_PRICE_PLAN_ID, Config.DEFAULT_USER_ID);
    
    assertResponseIsCreated(response);
    ProfileResponse profile = extractProfileFromResponse(response);
    
    assertNotNull(profile.getId());
    assertProfileMsisdn(profile, msisdn);
    assertEquals(profile.getPricePlanId(), Config.DEFAULT_PRICE_PLAN_ID);
}
```

**Что проверяется:**
- HTTP статус 201 Created
- Профиль получил уникальный ID
- MSISDN сохранен корректно
- Price Plan ID и User ID совпадают с ожидаемыми

### Пример 2: Проверка дубликата MSISDN (негативный тест)

```java
@Test
public void shouldReturnErrorWhenCreatingProfileWithDuplicateMsisdn() {
    String msisdn = MsisdnGenerator.generateUniqueMsisdn();
    
    // Создаем первый профиль
    profileApi.createProfile(msisdn, 
            Config.DEFAULT_PRICE_PLAN_ID, Config.DEFAULT_USER_ID);
    
    // Пытаемся создать второй с тем же MSISDN
    Response response = profileApi.createProfile(msisdn, 
            Config.DEFAULT_PRICE_PLAN_ID, Config.DEFAULT_USER_ID);
    
    assertResponseIsBadRequest(response);
}
```

**Что проверяется:**
- API не позволяет создать профиль с дубликатом номера
- Возвращается HTTP 400 Bad Request

### Пример 3: Обновление баланса

```java
@Test
public void shouldUpdateBalance() {
    // Создаем профиль
    String msisdn = MsisdnGenerator.generateUniqueMsisdn();
    Response createResponse = profileApi.createProfile(msisdn, 
            Config.DEFAULT_PRICE_PLAN_ID, Config.DEFAULT_USER_ID);
    Long profileId = extractProfileFromResponse(createResponse).getId();
    
    // Обновляем баланс
    BigDecimal newAmount = new BigDecimal("1500.50");
    Response updateResponse = balanceApi.updateBalance(profileId, newAmount);
    
    assertResponseIsOk(updateResponse);
    
    // Проверяем, что баланс обновился
    Response getResponse = balanceApi.getBalanceById(profileId);
    BalanceResponse balance = extractBalanceFromResponse(getResponse);
    assertEquals(balance.getAmount(), newAmount);
}
```

---

## 🚀 Технологический стек

### Основные зависимости

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| **TestNG** | 7.10.2 | Фреймворк для написания и запуска тестов |
| **REST Assured** | 5.5.0 | DSL для тестирования REST API |
| **Jackson** | 2.17.0 | Сериализация/десериализация JSON |
| **DataFaker** | 2.4.2 | Генерация тестовых данных |

### Плагины Maven

- `maven-compiler-plugin 3.13.0` — компиляция Java кода
- `maven-surefire-plugin 3.5.2` — запуск тестов

---

## 🔧 Запуск тестов

### Предварительные требования

- ☕ **Java 21** или выше
- 📦 **Maven 3.6+**
- 🌐 Доступ к тестовому API: `http://195.38.164.168:7173`

### Команды запуска

#### Запуск всех тестов
```bash
mvn clean test
```

#### Запуск конкретной группы тестов
```bash
# Только позитивные тесты профилей
mvn test -Dtest=ProfilePositiveTests

# Только негативные тесты балансов
mvn test -Dtest=BalanceNegativeTests

# Все тесты авторизации
mvn test -Dtest=AuthTests
```

#### Запуск через TestNG XML
```bash
mvn test -DsuiteXmlFile=testng.xml
```

---

## 📈 Преимущества фреймворка

### ✨ Удобство использования
- **Читаемый код**: понятные названия методов и классов
- **Минимум boilerplate**: благодаря BaseTest и API клиентам
- **Подробные комментарии**: каждый метод и класс документирован

### 🔄 Переиспользуемость
- **API клиенты**: одна точка изменений для эндпоинта
- **BaseTest**: общие проверки и утилиты доступны всем тестам
- **Config**: централизованное управление настройками

### 🛡️ Надежность
- **Автоматическая авторизация**: JWT токен получается перед запуском
- **Уникальные данные**: генератор MSISDN исключает конфликты
- **Проверка статусов**: встроенные assert'ы для HTTP ответов

### 📊 Масштабируемость
- **Модульная структура**: легко добавить новые API клиенты
- **Расширяемые DTO**: простое добавление новых полей
- **Гибкая конфигурация**: Config.java для разных окружений

---

## 🔍 Тестируемая функциональность

### Авторизация
- ✅ Регистрация нового пользователя
- ✅ Вход существующего пользователя
- ❌ Попытка регистрации с дубликатом username
- ❌ Вход с неверными учетными данными

### Профили абонентов
- ✅ CRUD операции (Create, Read, Update, Delete)
- ✅ Получение по ID и MSISDN
- ✅ Фильтрация (все профили / удаленные)
- ❌ Валидация обязательных полей
- ❌ Защита от дубликатов MSISDN
- ❌ Обработка несуществующих ID

### Балансы
- ✅ Получение баланса по ID профиля
- ✅ Обновление суммы на балансе
- ✅ Получение списка всех балансов
- ❌ Попытка обновить баланс несуществующего профиля
- ❌ Валидация некорректных сумм

### Счетчики (Counters)
- ✅ Получение счетчика по ID профиля
- ✅ Получение всех счетчиков
- ✅ Получение только активных счетчиков
- ❌ Обработка несуществующих ID

---

## 📚 Документация кода

Весь код покрыт **JavaDoc комментариями**:
- 📖 Описание классов и их назначения
- 📖 Описание методов и параметров
- 📖 Примеры использования
- 📖 Объяснение бизнес-логики

Пример документации:
```java
/**
 * Центральное хранилище всех конфигурационных констант для тестового фреймворка.
 * 
 * Этот класс содержит:
 * - URL адреса API эндпоинтов
 * - Данные для авторизации администратора
 * - Значения по умолчанию для создания тестовых данных
 */
public class Config {
    /**
     * Базовый URL тестируемого API.
     * Используется как префикс для всех HTTP запросов.
     * Например: BASE_URL + AUTH_LOGIN = "http://195.38.164.168:7173/api/auth/sign_in"
     */
    public static final String BASE_URL = "http://195.38.164.168:7173";
}
```

---

## 🎓 Применяемые паттерны и практики

### Design Patterns
- **Page Object Pattern** (адаптирован для API)
- **Builder Pattern** (в DTO для создания запросов)
- **Factory Pattern** (генераторы тестовых данных)

### Best Practices
- ✅ **Single Responsibility Principle** — каждый класс отвечает за одну область
- ✅ **DRY (Don't Repeat Yourself)** — переиспользование через BaseTest
- ✅ **Explicit is better than implicit** — понятные имена переменных
- ✅ **Fail Fast** — ранняя проверка ошибок в тестах

### Code Quality
- 📝 Подробная документация (JavaDoc)
- 🏗️ Четкая структура пакетов
- 🔤 Использование констант вместо magic values
- 🧹 Чистый и читаемый код

---

## 🎯 Применение в реальных проектах

Этот фреймворк может использоваться для:

1. **Regression Testing** — проверка работоспособности после изменений
2. **Smoke Testing** — быстрая проверка критической функциональности
3. **Integration Testing** — тестирование взаимодействия компонентов
4. **CI/CD Pipeline** — автоматический запуск в Jenkins/GitLab CI

### Интеграция с CI/CD

Пример конфигурации для GitHub Actions:
```yaml
name: API Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
      - name: Run tests
        run: mvn clean test
      - name: Publish Test Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: TestNG Tests
          path: target/surefire-reports/*.xml
          reporter: java-junit
```

---

## 📊 Метрики качества

### Покрытие

- **44 теста** покрывают основную функциональность
- **Позитивные + Негативные** сценарии для каждого модуля
- **~1400 строк** тестового кода

### Производительность

- ⚡ Быстрый запуск тестов благодаря REST Assured
- ⚡ Параллельное выполнение через TestNG (опционально)
- ⚡ Минимальные зависимости = быстрая сборка

---

## 🚧 Возможности для расширения

### Краткосрочные улучшения
- [ ] Добавить Allure отчеты для красивой визуализации
- [ ] Настроить параллельный запуск тестов
- [ ] Добавить логирование (Log4j/SLF4J)
- [ ] Создать Docker образ для изолированного запуска

### Долгосрочные улучшения
- [ ] Добавить тесты производительности (JMeter/Gatling)
- [ ] Интеграция с Test Management системами (TestRail, Zephyr)
- [ ] Мониторинг метрик API (response time, error rate)
- [ ] Контрактное тестирование (Pact)

---

## 👥 Команда и вклад

Этот фреймворк разработан для демонстрации навыков автоматизации API тестирования и может служить основой для реальных проектов.

### Основные принципы разработки
- 🎯 **Quality First** — качество кода важнее скорости
- 📚 **Documentation Matters** — код должен быть понятным
- 🔄 **Continuous Improvement** — постоянное совершенствование
- 🤝 **Collaboration** — готовность к работе в команде

---

## 📞 Контакты и ссылки

- **Repository**: [github.com/ArtemKuhestani/qabilling-test-framework](https://github.com/ArtemKuhestani/qabilling-test-framework)
- **API URL**: http://195.38.164.168:7173

---

## 🏆 Заключение

**QA Billing Test Framework** — это:
- ✅ **Профессиональный** подход к автоматизации API тестирования
- ✅ **Масштабируемое** решение для больших проектов
- ✅ **Готовый к использованию** фреймворк с документацией
- ✅ **Пример лучших практик** в QA автоматизации

Фреймворк демонстрирует глубокое понимание:
- REST API тестирования
- Паттернов проектирования
- Инструментов автоматизации (TestNG, REST Assured)
- Практик разработки тестов

---

*Спасибо за внимание! 🙏*

*Документ создан: Ноябрь 2025*
